<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.muslimart2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/muslimartlogo2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/muslimartlogo2"
        android:supportsRtl="true"
        android:theme="@style/Theme.Muslimart2"
        tools:replace="android:theme,android:icon">
        <activity android:name=".search"></activity>
        <activity android:name=".ShippingDetails" />
        <activity android:name=".PaymentMethod" />
        <activity android:name=".CartActivity" />
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".Admin_Add_New_Product" />
        <activity android:name=".Admin" />
        <activity android:name=".Login" />
        <activity android:name=".SignUp" />
        <activity android:name=".firstPage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>